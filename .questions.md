- am i logging info? or is that handled by the app
  - if so where should i output to
- return voltages as a tuple?
- for boardinfo struct, I am not sure what information you actually need. I just need to query the correct device member, but need to know more about which information you want.